generate array for nepali calendar

 - get (today | selected day's) date & type ie, english or nepali 

 - if nepali date, 
 	- convert to english

 - if english
 	- convert to nepali date

 - goto the 1st day of the selected nepali month

 - convert the 1st nepali date to english date

 - get its weekday, ie. the first weekday of the nepali month

 - if 1st weekday is not sunday
    - generate array to hold info. for 1 day :
		array(en_year,en_month,en_date,np_year,np_month,np_date,week_of_month)
 	- fill the array with blanks upto that day

 - loop through 1 to end of the month in nepali
    - generate array to hold info. for 1 day :
		array(en_year,en_month,en_date,np_year,np_month,np_date,week_of_month)
	- push that array into the array holding the month's info.


generate html : 
 - loop through the array
